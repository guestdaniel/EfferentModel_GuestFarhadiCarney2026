# Introduction
This is the "wrapper" code for the Guest, Farhadi, and Carney (2026) medial olivocochlear (MOC) reflex model, published in JASA.
The wrapper code makes it easy to generate model responses from the MOC model in the high-level programming languages MATLAB and Julia.

If you want to inspect the source code of the model and wrappers, refer to [File structure](#file-structure) below.
If you want to use the code to generate new model responses, refer to [Installation](#installation) and [Usage](#usage) below.

This and documentation is licensed under GNU AGPL v3.

# File structure
A subset of the files in this repository are listed below with explanations.
Key components are in the `src` folder.
`src/*.jl` files define the Julia package and the associated wrapper and C interface code for implementing the model.
The `src/model` folder provides a copy of the model source code, which is otherwise maintained at <https://github.com/guestdaniel/EfferentModelCarneyC>.
The `src/mex` folder contains the MATLAB and Mex files that define the MATLAB wrapper and interface code for implementing the model.
The `test` folder contains a test suite written in Julia that validates basic aspects of the model code and checks responses generated by the model against responses generated by older models to verify that new model components have not fundamentally altered established response characteristics.

```
EfferentModel_GuestFarhadiCarney2026/
├── Project.toml                           # Julia package configuration
├── Manifest.toml                          # Julia package manifest
├── src/                                   # Main Julia package source
│   ├── EfferentModel_GuestFarhadiCarney2026.jl  # Package entry point
│   ├── wrappers.jl                        # Julia model wrappers
│   ├── c_interface.jl                     # Julia model C interface
│   ├── utils.jl                           # TODO: DELETE
│   ├── model/                             # Model source code
│   │   ├── model.c                        # C source code for model
│   │   ├── model.h                        # C header file for model
│   │   ├── sfie.c                         # C implementation of SFIE model components
│   │   ├── adaptation.c                   # C source code for PLA
│   │   ├── complex.c                      # C source code for complex number handling
│   │   ├── compile.sh                     # Example compilation script
│   │   └── README.md                      # Model-level notes and changelog
│   └── mex/                               # MATLAB Mex interface
│       ├── sim_efferent_model.m           # MATLAB model wrappers function
│       ├── sim_efferent_model_mex.c       # Mex model interface 
│       ├── compile_mex.m                  # MATLAB compilation script
│       ├── demo.m                         # MATLAB demo script
│       └── ffGn.m                         # MATLAB source code for fGn synthesis
├── test/                                  # Julia test suite
│   └── runtests.jl                        # Primary testing script
└── external/                              # Reference model code
    ├── original/                          # ZBC 2014 baseline
    ├── original_expadapt_pla/             # ZBC 2014 baseline w/ new PLA approx.
    ├── efferent_farhadi/                  # Mex and C code for Farhadi et al. model
    └── julia/                             # Makeshift Julia interface for 2014 baseline
```

# Installation

## Julia

### Model compilation
For Julia, you first need to configure a C compiler; we use MinGW (on Windows) and gcc (on Linux):
1. Manually compile the model following the demo script  at `src/model/compile.sh`.
2. A successful compilation results in the shared object library artifact `libgfc2023.so`; verify that these files exist.

Please contact the authors for support.

### Package installation
Next, install the Julia wrapper package:
1. Type `]` in the REPL to activate the Pkg REPL.
2. In the Pkg REPL, type `add path_to_this_folder` to install the package in your environment.
3. Use the package with `using EfferentModel_GuestFarhadiCarney2026` in your Julia scripts.
4. Follow the [Usage](#usage) instructions below to invoke the model.

## MATLAB

### Model compilation
For MATLAB, you first need to configure a C compiler.

1. Set up a compiler or MATLAB compiler add-on for the version of MATLAB that you want to use.
On Windows, you *must* use the "MATLAB Support for MinGW-w64 C/C++/Fortran Compiler" add-on from the Add-on Explorer.
2. Type `mex -setup C` in the MATLAB Command Window to verify that MATLAB can locate the appropriate C compiler on your machine.
On Windows, if shown multiple options, you *must* select the "MinGW64 Compiler (C)" option and not any other options, such as "Microsoft Visual C++". 
This limitation arise from Microsoft's lack of support for variable-length arrays in their C compilers for Windows.

Next, run the compilation script: 
1. Change your working directory in MATLAB to the src/mex folder. Run >>compile_mex in the command window, or open compile_mex.m nad run it frmo the 'play' button in the editor.
A successful compilation results in Mex files being generated in the folder that are specific to your platform: `*.mexw64` for Windows, `*.mexa64` for Linux, or `*.mexmaci64` and `*.mexmaca64` for Intel and Apple Silicon MacOS, respectively.

Please contact the authors for support.

### Package installation
1. "Installation" in MATLAB consists merely of putting the `src/mex` folder in your MATLAB path.

Follow the [Usage](#usage) instructions below to invoke the model.

# Usage

## Julia
There are two main functions that users can call to generate model responses, `sim_gfc2023` and `sim_gfc2023_dict`:
- `sim_gfc2023`: Returns responses in the format `Vector{Vector{Vector{Float64}}}`. The outer dimension indexes 20 different model stages (listed below), the second dimension indexes CF channels, and the inner dimension indexes time.
- `sim_gfc2023_dict`: Returns responses in the format `Dict{String, Vector{Vector{Float64}}}`. Each dict value is the response in a single model stage. 

Otherwise, these two functions behave the same way and have the same arguments.
The following description will use `sim_gfc2023_dict` as an example.
The input arguments are structured as `sim_gfc2023_dict(x, cfs; kwargs...)` where `x` is a required positional argument containing a single time-pressure waveform in units of Pa and `cfs` is a required positional argument containing a vector of characteristic frequencies in units of Hz.
Other arguments are passed as optional keyword arguments.
For additional information about how to use the function, refer to the help by switching to the Help REPL via `?` in the regular REPL and then typing the name of the function.

## MATLAB
There is one function that users can call to generate model responses, `sim_efferent_model`.
This function takes two required arguments `x` and `cfs` where `x` is a required positional argument containing a single time-pressure waveform in units of Pa and `cfs` is a required positional argument containing a vector of characteristic frequencies in units of Hz. Note that the signal `x` must be created with a sampling rate that matches the model's sampling rate, `fs` (default/recommended rate is 100 kHz).
Other input arguments are passed as optional keyword arguments, using either the `func('key', 'value')` syntax or the `func(key=value)` syntax.
The function's output arguments are matrices of signals representing the responses of inner hair cells (ihcout), high-spontaneous-rate auditory-nerve (AN) fibers (hsrout), low-spontaneous-rate AN fibers (lsrout), and the time-varying cochlear gain factor (gain). 
For additional information about how to use the function, refer to the comment block via `help sim_efferent_model` and to the extensive demo script located at `src/mex/demo.m`.

## Tips and customization
This model was designed to replicate data from the auditory nerve in anesthetized cat.
Given that many researchers are interested in simulating responses in very different scenarios (e.g., awake, human), there may be interest in modifying the strength and CF-weighting of the MOC reflex in the model.
To do so, we recommend using the parameter `moc_beta`.
Its default value, which matches the value suggested in the 2026 JASA article, can be seen in the source code for the wrappers (`wrappers.jl` or `sim_efferent_model.m`) and combines an overall weight (`0.045`) with a CF-weighting function.
Increasing the overall weight will steepen the MOC-IO nonlinearity slope and cause the MOC reflex to engage at lower levels and increase more rapidly with sound level.

# Testing
The Julia package also includes an automated "test suite" of programs that test specific aspects of the model responses.
The majority of these tests compare model responses in the absence of MOC feedback (i.e., `moc_weight=0`) to the "baseline" model described in Zilany, Bruce, and Carney (2014)'s JASA article to quantify the extent to which modifications to the model code to add MOC feedback affected model responses in undesired ways.

To run the testing suite yourself... 
1. Open a Julia REPL to this folder and then open type `]` to access the Pkg REPL 
2. Inside the Pkg REPL, activate this environment by typing `activate .`
3. Inside the Pkg REPL, set this environment up by typing `instantiate`
4. Inside the Pkg REPL, run the test suite by typing `test`
